# Runtime dependencies. Test/dev tooling lives in requirements-dev.txt.
#
# numpy is capped at <3.0 rather than <2.0: the whole pipeline is verified
# running on numpy 2.x, and the old cap would have put CI on numpy 1.x while
# development ran on 2.x -- two different majors for the entire project history.
pandas>=2.0,<3.0
numpy>=1.24,<3.0
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.0,<3.0
streamlit>=1.35,<2.0
requests>=2.31,<3.0
